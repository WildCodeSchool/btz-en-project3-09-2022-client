export type TUser = {
  id: string;
  firstname: string;
  lastname: string;
  imageUrl: string;
  workLocation: string;
};

export type TSpace = {
  name: string;
  imageUrl: string;
  id: string;
};

export type TTeam = {
  name: string;
};
